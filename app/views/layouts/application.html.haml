!!!
%html
  %head
    %title SimpleBlog
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    #wrapper.center-block
      - if cookies[:views]%10 == 0
        #banner.center-block
          %h2 Learning it is light. Do this together with GeekHub!
      #header
        %h1
          Simple
          %span Blog
        = link_to "Back To Main Page", root_path, class: 'back_to_main' unless current_page?(root_path)
        .user_info
          - if current_user
            = current_user.name
            = current_user.email
            %span.v-bar
              &#124
            = link_to "Exit", sessions_logout_path
          - else
            = link_to "Sign In", sign_in_path
            %span.v-bar
              &#124
            = link_to "Register New User", sign_up_path
      #content
        = yield