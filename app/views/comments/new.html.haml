= form_for(@comment) do |f|
  = f.hidden_field :post_id, value: @post_id
  .form-group
    = f.label :comment
    = f.text_area :comment, class: 'form-control'
  = f.button :submit => "Add Comment", class: "btn btn-default"